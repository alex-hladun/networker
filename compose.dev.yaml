services:
  monitor:
    build:
      context: .
      target: dev
    image: networker-monitor-dev
    command: >-
      sh -c "pnpm install && pnpm dev --host 0.0.0.0 --port 3000 --strictPort"
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: 'true'
    volumes:
      - .:/app
      - monitor_dev_node_modules:/app/node_modules
    healthcheck:
      start_period: 45s

volumes:
  monitor_dev_node_modules:
